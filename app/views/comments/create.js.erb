$('#comments').append("<%= j render @comment %>"); // This renders the _comment partial in js format ad adds(appen) to end of comments list
$('#comment_body').val(""); //This empties the text field of the comment form after submission